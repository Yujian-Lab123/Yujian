# 个人画像地图设计验收

## 对照范围

- 视觉参照：`C:\Users\Administrator\AppData\Local\Temp\codex-clipboard-8cf8fefa-8bff-4e95-b896-7ef66c08403a.png`（1448 × 1086 px）
- 用户标注问题：`codex-clipboard-94af3e41-dc25-4981-b292-ea4b0d007dd7.png`（头像下方文案）与 `codex-clipboard-12fb64fa-ca2c-4a6f-ad96-3d1cf509bcdc.png`（第 5 维支线走向）。
- 最终实现：`design-qa/profile-self-organizing-desktop.png`（1264 × 1042 px）。
- 运行页面：`/profile` 初始人物画像；浏览器 CSS 视口 1280 × 720，设备 DPR 1.5。
- 归一化说明：参照图是 1448px 桌面稿；浏览器原始捕获受应用内浏览器 DPR 缩放影响，活动画面位于左上 842 × 694 px，已等比归一化为 1264 × 1042 px 后比较。比较重点是同一人物地图的结构、空间关系、文案留白和曲线走向。

已在同一轮检查中打开参照图和最终浏览器截图；并检查了中心肖像、左侧交流区、右侧三组维度、代表内容、时间节点和时间曲线。

## Findings

- 无遗留 P0/P1/P2。
- P3：当前碰撞规则按左、中、右语义链纵向消解，能够保留参考图的组织形式。未来若增加横跨多个分区的大型新模块，应先声明它属于哪一条语义链，而不是交给无约束力导向布局随机摆放。

## 本轮修复与证据

1. **P1：中等宽度裁切右侧模块**
   - 位置：`reference-canvas`。
   - 原因：1448px 绝对定位画布在 1280px 视口溢出，页面的横向裁切会隐藏右侧时间点和认识建议。
   - 修复：新增 `reference-map-stage`，由可用宽度计算等比缩放，并保持逻辑画布 1448 × 1020 坐标系。
   - 复核：最终页面 `scrollWidth` 1265px、页面宽度 1264.7px，无横向溢出；五条人物关系曲线均存在。

2. **P1：时间轴缺少连续叙事线**
   - 位置：`reference-timeline-rail`。
   - 原因：节点彼此孤立，无法表达“走过什么”的时间推进。
   - 修复：从五个真实时间圆点的 DOM 坐标生成平滑贝塞尔曲线，节点位置或内容重排后会与其他关系线一起重算。
   - 复核：最终页面检测到 5 个时间节点、1 条时间蜿蜒曲线和 5 条人物维度支线；曲线位于文字下层，不遮挡内容。

## Fidelity surfaces

- **字体与层级**：保留衬线标题、深蓝编号和低对比正文的层级；时间节点沿曲线错落，避免横排挤压。
- **间距与布局节奏**：中心人物—五个维度—底部证据层的空间关系成立；1280px 状态下无裁切、重叠或横向滚动。
- **颜色与视觉令牌**：延续暖纸底、淡金线条、深蓝维度编号和低透明度浅边框，时间线使用略深的淡金以保持可见但不抢占主体。
- **图像质量**：山水底图与水墨人物头像保持原有裁切与表现；曲线是基于数据与节点位置的功能性关系图，不替代任何参照图中的插画资产。
- **文案与交互**：核心摘要与中心判断不再硬截断，长文案会触发纵向延展；时间轴、交流特征和价值偏好仍使用受控视觉槽位，完整说明可在证据抽屉阅读。代表内容搜索与导航未因重排改动。

## 响应式与交互

- 桌面：1280px 宽下的完整地图已验证。
- 小于 1050px：仍切换为顺序阅读布局并隐藏连线图，避免交叉细线损害可读性。
- 已验证：渲染后有 5 条人物支线、1 条时间引线、1 条时间蜿蜒曲线和 10 个人物支线端点。三份现有画像工件分别渲染验证，关键分区交叠面积均为 0；逻辑画布高度分别自适应到 1118px、1076px、1075px。构建、TypeScript 与 ESLint 通过。

## Comparison history

1. 初版：地图结构建立，但 1280px 下右侧模块会被裁切，时间节点没有连续路径。
2. 修复：加入等比舞台容器、缩放坐标换算和动态时间蜿蜒曲线。
3. 修复后证据：`design-qa/profile-layout-winding-timeline-desktop.png`；无页面横向溢出，所有时间节点与右侧模块可见。
4. 文字留白修复：用户标注的交流标题、时间节点标题和肖像上方首段经历与曲线距离过近。交流标题上移 18px；时间轴文案下移 18px（首节点 28px）；人物到时间模块的分支改接入顶部留白。复核证据：`design-qa/profile-text-clearance-desktop.png` 及其三个区域裁切图。
5. 编号锚点修复：上一步仍把支线接在模块容器的空白坐标，导致编号与曲线视觉断开，同时第 5 项被局部位移挤回摘要区。现改为读取肖像圆框与 1–5 编号圆点的实际 DOM 中心：五条支线终点与编号中心测量误差均为 0px；第 5 项整体下移后与摘要保持 23.72px 间距。第 1 项另增加“编号 → 首个年份节点”的连续曲线。复核证据：`design-qa/profile-number-anchors-desktop.png`。
6. 肖像文案与第 5 维支线修复：中心判断移到头像下方并保持 17.47px 可见间距；价值支线从头像右下缘绕开中心文案；第 5 维支线的两个控制点上提，形成经过标题上方的自然弧线。复核证据：`design-qa/profile-self-organizing-desktop.png`。
7. 多用户自组织修复：移除核心摘要与中心判断的硬截断，新增基于 `ResizeObserver` 的语义链碰撞消解。左链按“摘要 → 交流 → 代表内容 → 精选内容”下推，右链按“关切 → 决策 → 价值 → 底部内容”下推，中部人物说明共同约束代表内容起点；画布与舞台高度随最终布局增长，曲线按移动后的实际锚点重算。三份画像样本的六组关键相邻区域交叠面积均为 0。

## Implementation checklist

- [x] 修正中等宽度的整页裁切。
- [x] 让人物支线在缩放与内容重排后继续连接正确节点。
- [x] 将时间轴替换为经过实际节点的平滑曲线。
- [x] 让长画像文案触发纵向延展和分区碰撞消解。
- [x] 使用三份现有画像工件验证自组织结果。
- [x] 运行 `npm run lint`、`npm run typecheck`、`npm run build`。

final result: passed
